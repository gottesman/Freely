# Freely environment configuration
# Copy this file to .env and adjust values for your setup
# Only variables prefixed with VITE_ are exposed to the renderer bundle

# --- Spotify (recommended: external token endpoint via Cloudflare Worker) ---
# OS-level env for Tauri/Rust/backend usage
SPOTIFY_TOKEN_ENDPOINT=
# Renderer override (optional). If set, AccessEnv will prefer this Vite-exposed value
VITE_SPOTIFY_TOKEN_ENDPOINT=

# --- Spotify Charts proxy (optional) ---
CHARTS_SPOTIFY_ENDPOINT=
VITE_CHARTS_SPOTIFY_ENDPOINT=

# --- Genius proxy (optional) ---
GENIUS_ENDPOINT=
VITE_GENIUS_ENDPOINT=

# --- App configuration ---
# Default market for Spotify public metadata lookups
SPOTIFY_DEFAULT_MARKET=US
VITE_SPOTIFY_DEFAULT_MARKET=US

# Optional custom user agent string
APP_USER_AGENT=FreelyPlayer/dev
VITE_APP_USER_AGENT=FreelyPlayer/dev

# Notes:
# - Use the OS-level variables for Tauri/Rust-side operations.
# - Use the VITE_* variants to override values in the renderer (AccessEnv reads VITE_* first).
# - Keep secrets out of the renderer; prefer proxies/token endpoints instead of local secrets.
