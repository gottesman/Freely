# Genius API configuration for Freely Player
# Copy this file to .env and fill in values. DO NOT commit your real secrets.
# Only variables prefixed with VITE_ are exposed to the renderer bundle.
# Keep CLIENT_SECRET private (not prefixed) and access it only in main / secure context.

# Public-ish (still sensitive) access token if you want to call Genius directly from renderer.
VITE_GENIUS_ACCESS_TOKEN=

# OAuth (Authorization Code Flow) values
GENIUS_CLIENT_ID=
GENIUS_CLIENT_SECRET=
GENIUS_REDIRECT_URI=http://localhost:5173/oauth/genius/callback

# Optional: custom UA string
VITE_APP_USER_AGENT=FreelyPlayer/0.3 (+https://example.local)

# Spotify API (Client Credentials for metadata lookups)
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
SPOTIFY_DEFAULT_MARKET=US

# Optional: external token endpoint (e.g. Cloudflare Worker) returning { access_token, expires_in }
SPOTIFY_TOKEN_ENDPOINT=
VITE_SPOTIFY_TOKEN_ENDPOINT=